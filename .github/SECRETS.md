# GitHub Secrets used by this repo

| Secret | Used in | Purpose |
|--------|---------|---------|
| `TG_BOT_TOKEN` | `release.yml` notify job | Post release notifications to operator's Telegram. Optional. |
| `TG_CHAT_ID`   | `release.yml` notify job | Target chat. |
